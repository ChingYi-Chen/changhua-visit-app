# auth.pyimport bcryptdef hash_password(password: str) -> str:    """    Return bcrypt hash string (utf-8) for storing in DB.    """    if password is None:        password = ""    pw = password.encode("utf-8")    hashed = bcrypt.hashpw(pw, bcrypt.gensalt(rounds=12))    return hashed.decode("utf-8")